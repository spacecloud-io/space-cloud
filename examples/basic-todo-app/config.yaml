
secret: some-secret
modules:
  crud:
    sql-mysql:
      conn: root:ATad2913-pw@/project
      collections:
        users:
          rules:
            read:
              rule: allow
            create:
              rule: allow
            update:
              rule: allow
            delete:
              rule: allow
          isRealtimeEnabled: false
      isPrimary: true
  auth:
    email:
      enabled: true

