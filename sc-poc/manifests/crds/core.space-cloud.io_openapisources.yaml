---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: openapisources.core.space-cloud.io
spec:
  group: core.space-cloud.io
  names:
    kind: OpenAPISource
    listKind: OpenAPISourceList
    plural: openapisources
    singular: openapisource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenAPISource is the schema for the openapisource API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OpenAPISourceSpec defines the desired state of OpenAPISourceSpec
            properties:
              openApi:
                description: OpenAPI describes the value/ref for openapi schema
                properties:
                  ref:
                    description: Ref stores the path from which openapi schema will
                      be fetched if value is not provided
                    properties:
                      method:
                        description: Method describes the http method we want to use
                          for the rest api
                        type: string
                      url:
                        description: Urk describes the http url we want to use for
                          the rest api
                        type: string
                    type: object
                  value:
                    description: Value stores the open api schema
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              source:
                description: Sources descrives the remote OpenAPI endpoints that we
                  want to federate
                properties:
                  url:
                    description: URL describes the full http url to communicate with
                      a remote http server
                    type: string
                required:
                - url
                type: object
            required:
            - openApi
            - source
            type: object
          status:
            description: OpenAPISourceStatus defines the observed state of OpenAPISource
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
