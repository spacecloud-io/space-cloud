---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: openapisources.core.space-cloud.io
spec:
  group: core.space-cloud.io
  names:
    kind: OpenAPISource
    listKind: OpenAPISourceList
    plural: openapisources
    singular: openapisource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenAPISource is the schema for the openapisource API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OpenAPISourceSpec defines the desired state of OpenAPISourceSpec
            properties:
              source:
                description: Sources descrives the remote OpenAPI endpoints that we
                  want to federate
                properties:
                  url:
                    description: URL describes the full http url to communicate with
                      a remote http server
                    type: string
                required:
                - url
                type: object
            required:
            - source
            type: object
          status:
            description: OpenAPISourceStatus defines the observed state of OpenAPISource
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
