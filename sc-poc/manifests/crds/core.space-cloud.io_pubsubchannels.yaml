---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: pubsubchannels.core.space-cloud.io
spec:
  group: core.space-cloud.io
  names:
    kind: PubsubChannel
    listKind: PubsubChannelList
    plural: pubsubchannels
    singular: pubsubchannel
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PubsubChannel is the schema for the pubsub channel
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PubsubChannel describes the specification of the pubsub channel
            properties:
              channel:
                description: Channel describes the name of the pubsub channel
                type: string
              consumerOptions:
                description: ConsumerOptions describes the options for a consumer
                properties:
                  plugins:
                    description: Plugins describes the plugins to be used for this
                      endpoint
                    items:
                      description: HTTPPlugin describes a plugin to be used in an
                        HTTP endpoint
                      properties:
                        driver:
                          description: Driver describes the driver to use for the
                            plugin
                          type: string
                        name:
                          description: Name describes a name of the plugin
                          type: string
                        params:
                          description: Params describes the additional properties
                            which are required by the driver
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - driver
                      - name
                      type: object
                    type: array
                type: object
              payload:
                description: Payload describes the payload schema of the channel
                properties:
                  additionalProperties:
                    description: AdditionalProperties defines if the schema accepts
                      properties other than the ones mentioned
                    format: byte
                    type: string
                  items:
                    description: Items list the items of the array
                  properties:
                    additionalProperties: {}
                    description: Properties list additional properties of the object
                    type: object
                  required:
                    description: Required specifies the required properties of the
                      object
                    items:
                      type: string
                    type: array
                  type:
                    description: Type defines the type of the data
                    type: string
                type: object
              producerOptions:
                description: ProducerOptions describes the options for a producer
                properties:
                  plugins:
                    description: Plugins describes the plugins to be used for this
                      endpoint
                    items:
                      description: HTTPPlugin describes a plugin to be used in an
                        HTTP endpoint
                      properties:
                        driver:
                          description: Driver describes the driver to use for the
                            plugin
                          type: string
                        name:
                          description: Name describes a name of the plugin
                          type: string
                        params:
                          description: Params describes the additional properties
                            which are required by the driver
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - driver
                      - name
                      type: object
                    type: array
                type: object
            required:
            - channel
            - payload
            type: object
          status:
            description: PubsubChannel defines the observed state of the pubsub channel
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
