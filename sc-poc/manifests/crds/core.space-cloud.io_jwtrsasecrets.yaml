---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: jwtrsasecrets.core.space-cloud.io
spec:
  group: core.space-cloud.io
  names:
    kind: JwtRSASecret
    listKind: JwtRSASecretList
    plural: jwtrsasecrets
    singular: jwtrsasecret
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: JwtRSASecret is the schema for the rsasecret api
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: JwtRSASecretSpec defines the desired state of JwtRSASecretSpec
            properties:
              allowedAudiences:
                description: AllowedAudiences is describes the allowed values in the
                  "aud" field of the jwt token.
                items:
                  type: string
                type: array
              allowedIssuers:
                description: AllowedIssuers describes the allowed values in the "iss"
                  field of the jwt token.
                items:
                  type: string
                type: array
              isPrimary:
                description: IsPrimary denotes if this secret is to be used as the
                  default secret
                type: boolean
              kid:
                description: The kid value of this secret
                type: string
              private:
                type: string
              public:
                description: Value holds the content of the secret or jwk url
                type: string
              valueFrom:
                description: Source for the AuthSecret's value. Cannot be used if
                  value is not empty.
                properties:
                  envRef:
                    description: EnvRef selects value from an environment variable
                    properties:
                      name:
                        description: Name of the environment variable
                        type: string
                    required:
                    - name
                    type: object
                  secretKeyRef:
                    description: SecretKeyRef selects a key of a secret in the pod's
                      namespace
                    properties:
                      key:
                        description: Key of the secret
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      source:
                        description: Source describes which secret manager to use.
                          By default its k8s
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
            required:
            - isPrimary
            - kid
            type: object
          status:
            description: JwtRSASecretStatus defines the observed state of RSASecret
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
