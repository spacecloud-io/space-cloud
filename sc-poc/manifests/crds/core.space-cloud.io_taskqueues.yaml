---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: taskqueues.core.space-cloud.io
spec:
  group: core.space-cloud.io
  names:
    kind: TaskQueue
    listKind: TaskQueueList
    plural: taskqueues
    singular: taskqueue
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TaskQueue is the schema for the TaskQueue source
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TaskQueueSpec describes the specification of a task queue
            properties:
              operations:
                description: Operations describes the options for all the operations
                properties:
                  ackTask:
                    description: AckTask describes the endpoint options for the "AckTask"
                      operation
                    properties:
                      plugins:
                        description: Plugins describes the plugins to be used for
                          this endpoint
                        items:
                          description: HTTPPlugin describes a plugin to be used in
                            an HTTP endpoint
                          properties:
                            driver:
                              description: Driver describes the driver to use for
                                the plugin
                              type: string
                            name:
                              description: Name describes a name of the plugin
                              type: string
                            params:
                              description: Params describes the additional properties
                                which are required by the driver
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - driver
                          - name
                          type: object
                        type: array
                    type: object
                  addTask:
                    description: AddTask describes the endpoint options for the "AddTask"
                      operation
                    properties:
                      plugins:
                        description: Plugins describes the plugins to be used for
                          this endpoint
                        items:
                          description: HTTPPlugin describes a plugin to be used in
                            an HTTP endpoint
                          properties:
                            driver:
                              description: Driver describes the driver to use for
                                the plugin
                              type: string
                            name:
                              description: Name describes a name of the plugin
                              type: string
                            params:
                              description: Params describes the additional properties
                                which are required by the driver
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - driver
                          - name
                          type: object
                        type: array
                    type: object
                  deleteTask:
                    description: DeleteTask describes the endpoint options for the
                      "DeleteTask" operation
                    properties:
                      plugins:
                        description: Plugins describes the plugins to be used for
                          this endpoint
                        items:
                          description: HTTPPlugin describes a plugin to be used in
                            an HTTP endpoint
                          properties:
                            driver:
                              description: Driver describes the driver to use for
                                the plugin
                              type: string
                            name:
                              description: Name describes a name of the plugin
                              type: string
                            params:
                              description: Params describes the additional properties
                                which are required by the driver
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - driver
                          - name
                          type: object
                        type: array
                    type: object
                  getPendingTasks:
                    description: GetPendingTasks describes the endpoint options for
                      the "GetPendingTasks" operation
                    properties:
                      plugins:
                        description: Plugins describes the plugins to be used for
                          this endpoint
                        items:
                          description: HTTPPlugin describes a plugin to be used in
                            an HTTP endpoint
                          properties:
                            driver:
                              description: Driver describes the driver to use for
                                the plugin
                              type: string
                            name:
                              description: Name describes a name of the plugin
                              type: string
                            params:
                              description: Params describes the additional properties
                                which are required by the driver
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - driver
                          - name
                          type: object
                        type: array
                    type: object
                  getTasks:
                    description: ReceiveTasks describes the endpoint options for the
                      "ReceiveTasks" operation
                    properties:
                      plugins:
                        description: Plugins describes the plugins to be used for
                          this endpoint
                        items:
                          description: HTTPPlugin describes a plugin to be used in
                            an HTTP endpoint
                          properties:
                            driver:
                              description: Driver describes the driver to use for
                                the plugin
                              type: string
                            name:
                              description: Name describes a name of the plugin
                              type: string
                            params:
                              description: Params describes the additional properties
                                which are required by the driver
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - driver
                          - name
                          type: object
                        type: array
                    type: object
                type: object
              source:
                description: Source describes which source to select as a source for
                  the task queue
                type: string
            required:
            - source
            type: object
          status:
            description: TaskQueueStatus defines the observed state of the TaskQueue
              source
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
